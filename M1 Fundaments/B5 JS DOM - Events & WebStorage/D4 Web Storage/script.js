window.onload = () => {
    function selectBGColor(color) {
        let content = document.querySelector('.content');
        content.style.backgroundColor = color;
        localStorage.setItem('backgroundColor', color);
    }
    let bgColorBtn = document.querySelectorAll(#background)
}